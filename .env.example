# EmailJS credentials for the Contact section.
# Create a free account at https://www.emailjs.com, then fill these in.
# Without them, the contact form gracefully falls back to opening the
# visitor's email client instead.

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
